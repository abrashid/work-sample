### AZURE DEVOPS PIPELINE
name: $(BuildDefinitionName)

trigger:
  branches:
    include:
       - development

### CONFIGURATION - EDIT ACCORDINGLY ###
parameters:
- name: region
  type: string
  default: eu-west-1
  values:
  - eu-west-1
- name: stacksConfig
  type: object
  default:
  - name: ebs

### DO NOT EDIT ###
resources:
  repositories:
    - repository: repo-name
      type: git
      name: ADO_PIPELINES

extends:
  template: templates/pipeline-core.yml@repo-name
  parameters:
    stacksConfig: ${{ parameters.stacksConfig }}
    region: ${{ parameters.region }}
